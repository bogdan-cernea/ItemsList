project 'ItemsList.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ItemsList' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ItemsList
  pod 'SDWebImage', '~> 5.0'
  pod 'SwiftLint'
  pod 'SwiftyJSON', '~> 4.0'
  
  target 'ItemsListTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ItemsListUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
